import { ArrangeMap } from "@/types/config";

export const arrangeMap = {
  openPeriod: {
    selector: '.pagelimitview',
    className: 'w-fit me-auto px-4 py-2 rounded-md text-lg bg-primary',
  },
} satisfies ArrangeMap
