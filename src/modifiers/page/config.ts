import { defineArrangeMap } from "@/types/config";

export const arrangeMap = defineArrangeMap({
  openPeriod: {
    selector: '.pagelimitview',
    className: 'w-fit me-auto px-4 py-2 rounded-lg text-lg bg-primary',
  }
})
